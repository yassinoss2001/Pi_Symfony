{% extends 'base.html.twig' %}

{% block title %}Supplement index{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <style>
        .table-container {
            max-width: 800px;
            margin: 0 auto;
        }
        .table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 20px;
        }
        .table th, .table td {
            border: 1px solid #dee2e6;
            padding: 8px;
            text-align: left;
        }
        .table th {
            background-color: #f8f9fa;
            color: #343a40;
            font-weight: 600;
        }
        .table-striped tbody tr:nth-of-type(odd) {
            background-color: rgba(0, 0, 0, 0.05);
        }
        .btn-show {
            background-color: #fd7e14;
            border-color: #fd7e14;
        }
        .btn-edit {
            background-color: #007bff;
            border-color: #007bff;
        }
        .btn-create {
            background-color: #28a745;
            border-color: #28a745;
        }
        .create-new-container {
            text-align: center;
            margin-top: 20px;
        }
        .title-container {
            text-align: center;
            margin-bottom: 20px;
        }
    </style>
{% endblock %}

{% block body %}
    <div class="title-container">
        <h1>Vos supplements</h1>
    </div>

    <div class="table-container">
        <table class="table table-bordered table-striped">
            <thead>
                <tr>
                    <th>Id</th>
                    <th>Nom</th>
                    <th>Prix</th>
                    <th>Image</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                {% for supplement in supplements %}
                <tr>
                    <td>{{ supplement.id }}</td>
                    <td>{{ supplement.nom }}</td>
                    <td>{{ supplement.prix }}</td>
                    <td>{{ supplement.image }}</td>
                    <td>
                        <button class="btn btn-sm btn-show"><a href="{{ path('app_supplement_show', {'id': supplement.id}) }}" style="color: white;">Show</a></button>
                        <button class="btn btn-sm btn-edit"><a href="{{ path('app_supplement_edit', {'id': supplement.id}) }}" style="color: white;">Edit</a></button>
                    </td>
                </tr>
                {% else %}
                <tr>
                    <td colspan="5">No records found</td>
                </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>

    <div class="create-new-container">
        <button class="btn btn-primary btn-create"><a href="{{ path('app_supplement_new') }}" style="color: white;">Create new</a></button>
    </div>
{% endblock %}
