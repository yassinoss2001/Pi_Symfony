{% extends 'base.html.twig' %}

{% block title %}Supplement{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <style>
        .supplement-details-container {
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
            border: 1px solid #ccc;
            border-radius: 5px;
            background-color: #f8f9fa;
        }
        .supplement-details-container h1 {
            text-align: center;
            margin-bottom: 20px;
        }
        .supplement-details-table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 20px;
        }
        .supplement-details-table th, .supplement-details-table td {
            border: 1px solid #dee2e6;
            padding: 10px;
            text-align: left;
        }
        .supplement-details-table th {
            background-color: #f8f9fa;
            color: #343a40;
            font-weight: 600;
        }
        .btn-container {
            text-align: center;
            margin-top: 20px;
        }
        .btn-container a {
            margin: 0 10px;
            text-decoration: none;
            padding: 10px 20px;
            border-radius: 5px;
        }
        .btn-back {
            background-color: #007bff;
            color: white;
        }
        .btn-edit {
            background-color: #28a745;
            color: white;
        }
        .btn-delete {
            background-color: #dc3545;
            color: white;
        }
    </style>
{% endblock %}

{% block body %}
    <div class="supplement-details-container">
        <h1>Détails du supplement</h1>

        <table class="supplement-details-table">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ supplement.id }}</td>
                </tr>
                <tr>
                    <th>Nom</th>
                    <td>{{ supplement.nom }}</td>
                </tr>
                <tr>
                    <th>Prix</th>
                    <td>{{ supplement.prix }}</td>
                </tr>
                <tr>
                    <th>Image</th>
                    <td><img src="{{ supplement.image }}" alt="{{ supplement.nom }}" style="max-width: 100%;"></td>
                </tr>
            </tbody>
        </table>

        <div class="btn-container">
            <a href="{{ path('app_supplement_index') }}" class="btn btn-back">Back to List</a>
            <a href="{{ path('app_supplement_edit', {'id': supplement.id}) }}" class="btn btn-edit">Edit</a>
            <a href="{{ path('app_supplement_delete', {'id': supplement.id}) }}" class="btn btn-delete">Delete</a>
        </div>
    </div>
{% endblock %}
